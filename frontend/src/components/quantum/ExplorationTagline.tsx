/**
 * Two-line tagline rendered near the bottom of the brand panel.
 * The word "exploration." is highlighted with the muted sage color.
 */
export function ExplorationTagline() {
  return (
    <p className="text-center text-[22px] leading-snug text-ink">
      Quantum learning
      <br />
      through <span className="text-sage">exploration.</span>
    </p>
  );
}
